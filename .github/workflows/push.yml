name: Push

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  lint:
    uses: ./.github/workflows/lint.yml
  push:
    needs:
      - lint
    uses: ./.github/workflows/build.yml
